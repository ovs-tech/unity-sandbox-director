# Tasks: Refactor SceneSandboxBuilder Cleanup

1. Identify all fallback logic, unused properties, and unused methods in SceneSandboxBuilder.
2. Remove fallback logic that is no longer required after refactor.
3. Remove unused properties and fields.
4. Remove unused methods and helper functions.
5. Validate that the class builds and runs without errors.
6. Update documentation/comments if needed.
7. Run tests and verify scene sandbox functionality is unaffected.
